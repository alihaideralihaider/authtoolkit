# Product Parts Source Stubs

This directory contains experimental TypeScript scaffolds for reusable AuthToolkit product parts.

These modules are intentionally isolated:

- no live route imports
- no database calls
- no external API calls
- no production side effects

Each folder should define typed inputs, typed outputs, and a placeholder `run*` function until the product part has a real API contract and implementation plan.

